<p id='notice'><%= notice%></p>

<div class="season">
<p>
  <strong>Name:</strong>
  <%= @event.name %><br>
  <strong>Location:</strong>
  <%= @event.location %><br>
  <strong>Start:</strong>
  <%= @event.date %><br>
</p>

  <div class="season_tools">
    <div class="seasons_buttons">
      <%= link_to 'Members', members_admin_event_path, class: 'btn btn-success' %>
      <div></div>
      <%= link_to 'Edit', edit_admin_event_path(@event), class: 'btn btn-success' %>
      <div></div>
      <%= link_to 'Back', admin_events_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>