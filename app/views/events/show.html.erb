<p id='notice'><%= notice%></p>
<div class="season">
<p>
  <strong>Name:</strong>
  <%= @event.name %><br>
  <strong>Location:</strong>
  <%= @event.location %><br>
  <strong>Start:</strong>
  <%= @event.date %><br>
</p>

  <div class="season_tools">
    <div class="seasons_buttons">
      <%= link_to 'Members', members_event_path, class: 'btn btn-success' %>
      <div></div>
      <%= link_to 'Back', events_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>